import Heading from "../../Layout/Heading";

const JobList = ({ jobListData }: { jobListData: any }) => {
  return (
    <div>
      <Heading text="All Jobs" />
    </div>
  );
};

export default JobList;
